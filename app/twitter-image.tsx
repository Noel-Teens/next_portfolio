// The Twitter/X share card reuses the Open Graph image verbatim — same 1200×630
// console card. Re-exporting keeps a single source of truth for the artwork.
export { alt, size, contentType, default } from "./opengraph-image";
